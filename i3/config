# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# This is how I disable nautilus desktop
# gconftool-2 --type bool --set /apps/nautilus/preferences/show_desktop false
# in gnome 3 this should be used instead:
# gsettings set org.gnome.desktop.background show-desktop-icons false

# Note: this config target Dvorak for programmers keyboard layout
focus_follows_mouse no

# popup_during_fullscreen smart

# Load my keymap
exec my-keyboard
exec_always  --no-startup-id ~/bin/wm_autostart

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
font pango:Droid Sans Mono 9
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec roxterm

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+semicolon exec dmenu_run

# start pomodoro-grabber
bindsym $mod+Shift+a exec pomodoro-grabber

# screenshot
bindsym Print exec screenshot.sh root
bindsym $mod+Print exec screenshot.sh active

# F1-F12 groups
## group 1: Internet
bindsym $mod+F1 exec firefox
bindsym $mod+F2 exec thunderbird
#claws-mail
bindsym $mod+F3 exec emacs
#bindsym $mod+F4 exec

## group 2: Local
bindsym $mod+F5 exec emacs
bindsym $mod+F6 exec caja #nautilus
bindsym $mod+F7 exec spacefm
bindsym $mod+F8 exec audacious

# bindsym $mod+F6 exec geany

## group 3: Utils
bindsym $mod+F9 exec stardict
bindsym $mod+F10 exec ibus-daemon --replace
bindsym $mod+F11 exec xournal
bindsym $mod+F12 exec cherrytree

# change focus
bindsym $mod+h focus left
bindsym $mod+t focus down
bindsym $mod+n focus up
bindsym $mod+s focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left
bindsym $mod+Shift+T move down
bindsym $mod+Shift+N move up
bindsym $mod+Shift+S move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+d split h

# split in vertical orientation
bindsym $mod+k split v

# enter fullscreen mode for the focused container
bindsym $mod+u fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+o layout stacking
bindsym $mod+comma layout tabbed
bindsym $mod+period layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# switch to workspace

bindsym $mod+ampersand workspace 1: [RE]search
bindsym $mod+bracketleft workspace 2: [C]ommunication
bindsym $mod+braceleft workspace 3: [O]rganization
bindsym $mod+braceright workspace 4: [M]ulti[M]edia
bindsym $mod+parenleft workspace 5: [EN]tertainment
bindsym $mod+equal workspace 6: [D]evelopment
bindsym $mod+asterisk workspace 7
bindsym $mod+parenright workspace 8
bindsym $mod+plus workspace 9
bindsym $mod+bracketright workspace 10

# move focused container to workspace
bindsym $mod+Shift+percent move container to workspace 1: [RE]search
bindsym $mod+Shift+7 move container to workspace 2: [C]ommunication
bindsym $mod+Shift+5 move container to workspace 3: [O]rganization
bindsym $mod+Shift+3 move container to workspace 4: [M]ulti[M]edia
bindsym $mod+Shift+1 move container to workspace 5: [EN]tertainment
bindsym $mod+Shift+9 move container to workspace 6: [D]evelopment
bindsym $mod+Shift+0 move container to workspace 7
bindsym $mod+Shift+2 move container to workspace 8
bindsym $mod+Shift+4 move container to workspace 9
bindsym $mod+Shift+6 move container to workspace 10

# reload the configuration file
bindsym $mod+Shift+J reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+P restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+greater exit
bindsym $mod+Shift+q exec ~/bin/off

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym t resize grow height 10 px or 10 ppt
        bindsym n resize shrink height 10 px or 10 ppt
        bindsym s resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+p mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
#        status_command i3status
        status_command showtime
        position top
}

# enable floating mode for some apps
for_window [class="Gimp"] floating enable
for_window [class="Audacious"] floating enable
for_window [class="Nautilus"] floating enable
for_window [class="Lxterminal"] floating enable
for_window [class="Stardict"] floating enable
for_window [class="synfigtudio"] floating enable
for_window [class="pinentry"] floating enable
for_window [class="Gnote"] floating enable
for_window [class="wbar"] floating enable
for_window [class="Cherrytree"] floating enable
for_window [class="barrydesktop"] floating enable
# To get the class and instance, you can use `xprop`

# Assign apps to workspace 1:
assign [class="Firefox"] → 1: [RE]search
assign [class="Chromium"] → 1: [RE]search
assign [class="Midori"] → 1: [RE]search

# workspace 2:
assign [class="Claws-mail"] → 2: [C]ommunication
assign [class="Thunderbird"] → 2: [C]ommunication
assign [class="Pidgin"] → 2: [C]ommunication

# workspace 3:
assign [class="File-roller"] → 3: [O]rganization
assign [class="Spacefm"] → 3: [O]rganization
assign [class="libreoffice"] → 3: [O]rganization
assign [class="Xournal"] → 3: [O]rganization
assign [class="Cherrytree"] → 3: [O]rganization

# workspace 4:
assign [class="Gimp"] → 4: [M]ulti[M]edia
assign [class="Openshot"] → 4: [M]ulti[M]edia
assign [class="Kino"] → 4: [M]ulti[M]edia
assign [class="Inkscape"] → 4: [M]ulti[M]edia
assign [class="Synfigstudio"] → 4: [M]ulti[M]edia

# workspace 5:
assign [class="smplayer"] → 5: [EN]tertainment
assign [class="Audacious"] → 5: [EN]tertainment
assign [class="Musique"] → 5: [EN]tertainment
assign [class="Perroquet"] → 5: [EN]tertainment
assign [class="VLC"] → 5: [EN]tertainment

# workspace 6:

assign [class="Geany"] → 6: [D]evelopment
assign [class="Emacs"] → 6: [D]evelopment
assign [class="Roxterm"] → 6: [D]evelopment
assign [class="ltbin"] → 6: [D]evelopment
